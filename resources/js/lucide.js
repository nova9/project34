import { createIcons, LogOut, CircleUser, Package, UserRound, LayoutDashboard } from 'lucide';

createIcons({
    icons: {
        LogOut,
        CircleUser,
        Package,
        UserRound,
        LayoutDashboard,
    },
    attrs: {
        'stroke-width': 1.5,
    },
});
